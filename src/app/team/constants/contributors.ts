import { TeamCardProps } from '../components/TeamCard';

const contributors: Omit<TeamCardProps, 'image'>[] = [
  {
    name: 'Contributor',
    title: 'Passionate Web3 Builder',
  },
];

export default contributors;
